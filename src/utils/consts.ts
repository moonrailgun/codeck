export const STANDARD_PIN_EXEC_IN = '$pin_exec_in';

export const STANDARD_PIN_EXEC_OUT = '$pin_exec_out';

export const variableTypes = [
  'string',
  'number',
  'boolean',
  'number',
  'object',
  'array',
] as const;
